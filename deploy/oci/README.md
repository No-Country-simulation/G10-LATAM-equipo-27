# Despliegue en OCI (Always Free)

Guía paso a paso para desplegar el `discord_extractor` en una VM Compute Always Free de
Oracle Cloud Infrastructure, con el paquete generado persistido en un bucket de Object Storage.

> En construcción. Se completa junto con el Dockerfile y la configuración de `storage.py`
> del servicio `discord_extractor`.
